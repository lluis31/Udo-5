using System;
namespace ex1_Udo5
{
    public class Circulo
    {
        public Circulo(double radioCirculo)
        {
            static double areaCirculo(double radioCirculo);
            return Math.Pow(radioCirculo, 2) * Math.PI;
        }
    }
}
